import { describe, expect, it } from 'vitest'; import { readFile } from 'node:fs/promises'; import { apply, profile_lab_gate } from '../src/plugin/index.js';
describe('package surface',()=>it('ships one bundle row and three tools',async()=>{const patch=await readFile('cordis.patch.yml','utf8');expect(patch).toContain('dsh-profile-lab');const names:string[]=[];apply({tool:(n)=>names.push(n)});apply({});expect(names).toEqual(['profile_lab_run','profile_lab_compare','profile_lab_gate']);await expect(profile_lab_gate()).rejects.toThrow('explicit policy');}));
