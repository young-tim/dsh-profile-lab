#!/usr/bin/env node
import { readFile } from 'node:fs/promises'; import path from 'node:path'; import YAML from 'yaml'; import { loadExperiment } from './config/index.js'; import { run } from './runner/index.js'; import { report } from './report/index.js'; import { gate } from './gate/index.js';
export const main=async(argv:string[]=process.argv.slice(2))=>{const a=[...argv],cmd=a.shift(),value=(k:string)=>a[a.indexOf(k)+1];if(cmd==='schema'){await loadExperiment(value('--check')!);console.log('schema valid');return 0;}if(cmd!=='run'&&cmd!=='compare'&&cmd!=='gate')throw new Error('unknown command');const dir=value('--output')??a[0];if(!dir)throw new Error('output required');const journal=path.join(dir,'journal.json');if(cmd==='run'){const e=await loadExperiment(a[0]!);e.workspace_template=path.resolve(e.workspace_template);await run(e,dir,value('--driver')!);console.log('run complete');return 0;}const cells=JSON.parse(await readFile(journal,'utf8'));const e=await loadExperiment('examples/experiment.yml');const r=await report(dir,e,cells);if(cmd==='compare'){console.log('reports written');return 0;}const p=YAML.parse(await readFile(value('--policy')!,'utf8'));const b=r.variants.find(x=>x.variant===e.baseline)!;const c=r.variants.find(x=>x.variant!==e.baseline)!;const reasons=gate(b,c,p);if(reasons.length){console.error(reasons.join('; '));return 1;}console.log('gate passed');return 0;};
if (process.argv[1] && import.meta.url.endsWith(process.argv[1])) main().then(code=>{process.exitCode=code;}).catch(x=>{console.error(x.message);process.exitCode=2;});
