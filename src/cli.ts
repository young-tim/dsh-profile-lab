#!/usr/bin/env node
import { readFile } from 'node:fs/promises'; import path from 'node:path'; import YAML from 'yaml'; import { loadExperiment } from './config/index.js'; import { run } from './runner/index.js'; import { report } from './report/index.js'; import { gate } from './gate/index.js';
const a=process.argv.slice(2), cmd=a.shift(), value=(k:string)=>a[a.indexOf(k)+1]; const main=async()=>{if(cmd==='schema'){await loadExperiment(value('--check')!);console.log('schema valid');return;}const dir=value('--output')??a[0];if(!dir)throw new Error('output required');const journal=path.join(dir,'journal.json');if(cmd==='run'){const e=await loadExperiment(a[0]!);e.workspace_template=path.resolve(e.workspace_template);await run(e,dir,value('--driver')!);console.log('run complete');return;}const cells=JSON.parse(await readFile(journal,'utf8'));const e=await loadExperiment('examples/experiment.yml');const r=await report(dir,e,cells);if(cmd==='compare'){console.log('reports written');return;}if(cmd==='gate'){const p=YAML.parse(await readFile(value('--policy')!,'utf8'));const b=r.variants.find(x=>x.variant===e.baseline)!;const c=r.variants.find(x=>x.variant!==e.baseline)!;const reasons=gate(b,c,p);if(reasons.length){console.error(reasons.join('; '));process.exitCode=1;}else console.log('gate passed');}};main().catch(x=>{console.error(x.message);process.exitCode=2;});
