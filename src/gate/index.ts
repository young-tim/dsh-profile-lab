import type { GatePolicy, Summary } from '../types.js'; export const gate=(base:Summary,candidate:Summary,p:GatePolicy)=>{const r:string[]=[];if(p.min_candidate_pass_rate!==undefined&&candidate.pass_rate<p.min_candidate_pass_rate)r.push('candidate pass rate below minimum');if(p.max_pass_rate_drop_pp!==undefined&&(base.pass_rate-candidate.pass_rate)*100>p.max_pass_rate_drop_pp)r.push('pass rate drop exceeds policy');if(p.max_error_rate!==undefined&&candidate.error_rate>p.max_error_rate)r.push('error rate exceeds policy');return r;};
