import { readFile } from 'node:fs/promises';
import type { Cell } from '../types.js';
export type SessionEvent = { type: string; status?: string; text?: string; usage?: { input?: number; output?: number; reasoning?: number; cache?: number }; duration_ms?: number; steps?: number; error?: unknown };
export const parseJsonl = (text: string): SessionEvent[] => text.split(/\r?\n/).filter(Boolean).flatMap(line => { try { const x = JSON.parse(line); return typeof x.type === 'string' ? [x as SessionEvent] : []; } catch { return []; } });
export const readSession = async (file: string) => parseJsonl(await readFile(file, 'utf8'));
export const projectCell = (id: Omit<Cell, 'status'|'duration_ms'|'steps'|'tool_calls'|'tool_errors'|'input_tokens'|'output_tokens'|'reasoning_tokens'|'cache_tokens'|'final_output_hash'|'evidence'|'attempts'>, events: SessionEvent[], evidence: string): Cell => { const end=events.findLast(x=>x.type==='turn/end'); const u=events.filter(x=>x.usage).reduce((a,x)=>({input:a.input+(x.usage?.input??0),output:a.output+(x.usage?.output??0),reasoning:a.reasoning+(x.usage?.reasoning??0),cache:a.cache+(x.usage?.cache??0)}),{input:0,output:0,reasoning:0,cache:0}); const final=events.findLast(x=>x.type==='assistant/final')?.text??''; return {...id,status:end?.status==='ok'?'pass':end?.status==='cancelled'?'cancelled':end?.status==='fail'?'fail':'error',attempts:1,duration_ms:end?.duration_ms??0,steps:end?.steps??events.filter(x=>x.type==='step/end').length,tool_calls:events.filter(x=>x.type==='tool/call').length,tool_errors:events.filter(x=>x.type==='tool/error').length,input_tokens:u.input,output_tokens:u.output,reasoning_tokens:u.reasoning,cache_tokens:u.cache,final_output_hash:awaitHash(final),evidence}; };
import { createHash } from 'node:crypto'; const awaitHash=(x:string)=>createHash('sha256').update(x).digest('hex');
