# Applied by DSH after the selected profile bundle. This intentionally adds one
# uniquely named loader row; the plugin exposes the three application tools.
- insert:
    - id: dsh-profile-lab
      name: dsh-profile-lab
